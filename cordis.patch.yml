# Applied as a bundle layer when a profile lists `@tomowang/dsh-data-agent`
# (see package.json → dsh.bundle.patch).
- insert:
    - id: data-agent
      name: '@tomowang/dsh-data-agent'
      config:
        defaultMaxRows: 500
